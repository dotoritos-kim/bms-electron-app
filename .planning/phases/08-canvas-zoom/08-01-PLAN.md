---
phase: 08-canvas-zoom
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - c:/SourceCode/bms-editor/src/chart/editor/types.ts
  - c:/SourceCode/bms-editor/src/chart/NoteChartEditor.tsx
  - c:/SourceCode/bms-editor/src/chart/editor/EditorToolbar.tsx
  - c:/SourceCode/bms-electron-app/src/renderer/routes/Editor.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "ZoomIn/ZoomOut buttons visible in EditorToolbar between H: slider and Keys: selector"
    - "Zoom range expanded from 5~80 to 2~200 px/beat"
    - "Ctrl+Wheel uses multiplicative step (x1.15) with cursor-anchored zoom"
    - "Zoom presets available: Overview(5), Work(20), Detail(80) via number click dropdown"
    - "Fit-to-Chart button auto-calculates scale to show entire chart"
    - "ZoomIn button disabled at max=200, ZoomOut disabled at min=2"
    - "Keymode-based default zoom applied on first load"
    - "onBeatScaleChange callback fires (rAF-debounced) so toolbar displays current scale"
  artifacts:
    - path: "c:/SourceCode/bms-editor/src/chart/editor/types.ts"
      provides: "ZoomControl interface + NoteChartEditorProps zoom props + EditorToolbarProps zoom props"
      contains: "ZoomControl"
    - path: "c:/SourceCode/bms-editor/src/chart/NoteChartEditor.tsx"
      provides: "cursor-anchored zoom, expanded bounds, keymode defaults, zoomControlRef, onBeatScaleChange"
      contains: "defaultBeatScaleForKeyMode"
    - path: "c:/SourceCode/bms-editor/src/chart/editor/EditorToolbar.tsx"
      provides: "Zoom button group (ZoomOut, scale display, ZoomIn, Fit)"
      contains: "showZoomPreset"
    - path: "c:/SourceCode/bms-electron-app/src/renderer/routes/Editor.tsx"
      provides: "zoomControlRef + currentBeatScale state wiring"
      contains: "zoomControlRef"
  key_links:
    - from: "Editor.tsx EditorToolbar"
      to: "zoomControlRef"
      via: "onZoomIn/onZoomOut/onZoomPreset/onZoomFit callbacks"
      pattern: "zoomControlRef\\.current"
    - from: "NoteChartEditor.tsx EditorCanvas"
      to: "zoomControlRef"
      via: "useEffect zoomControlRef.current = { zoomIn, zoomOut, zoomTo, fitToChart }"
      pattern: "zoomControlRef\\.current = \\{"
---

<objective>
에디터 캔버스 줌(확대/축소) 컨트롤 전체 개선.

현재: Ctrl+Wheel 단독 (±2 선형, 범위 5~80), 버튼 없음, 뷰 점프 발생.
목표: 툴바 줌 버튼 + 프리셋 + Fit-to-Chart + 커서 고정 줌 + 범위 확장(2~200) + 키모드 기본값 + 배율 표시.

Output: 4개 파일 수정 (types.ts, NoteChartEditor.tsx, EditorToolbar.tsx, Editor.tsx).
</objective>

<tasks>

<task type="auto">
  <name>Task 1: types.ts — ZoomControl 인터페이스 + 줌 props 추가</name>
  <files>c:/SourceCode/bms-editor/src/chart/editor/types.ts</files>
  <read_first>
    - c:/SourceCode/bms-editor/src/chart/editor/types.ts
  </read_first>
  <action>
**A. ZoomControl 인터페이스 추가** — CoordConverter 인터페이스 바로 아래에 추가:

```typescript
/** 줌 컨트롤 API (zoomControlRef용) */
export interface ZoomControl {
  zoomIn: () => void;
  zoomOut: () => void;
  zoomTo: (scale: number) => void;
  fitToChart: () => void;
}
```

**B. NoteChartEditorProps에 줌 props 추가** — `scrollBeatImperativeRef` 바로 아래에:

```typescript
/** 줌 컨트롤 imperative ref (zoomIn/zoomOut/zoomTo/fitToChart) */
zoomControlRef?: React.MutableRefObject<ZoomControl | null>;
/** 줌 배율 변경 콜백 (rAF 디바운스로 호출) */
onBeatScaleChange?: (scale: number) => void;
```

**C. EditorToolbarProps에 줌 props 추가** — `onLayerLockToggle` 아래에:

```typescript
// Zoom
onZoomIn?: () => void;
onZoomOut?: () => void;
onZoomPreset?: (scale: number) => void;
onZoomFit?: () => void;
currentBeatScale?: number;
```
  </action>
  <verify>
    <automated>cd c:/SourceCode/bms-editor && npx tsc --noEmit --skipLibCheck 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - types.ts contains `export interface ZoomControl`
    - types.ts NoteChartEditorProps contains `zoomControlRef`
    - types.ts NoteChartEditorProps contains `onBeatScaleChange`
    - types.ts EditorToolbarProps contains `onZoomIn`
    - types.ts EditorToolbarProps contains `currentBeatScale`
    - TypeScript passes with no errors
  </acceptance_criteria>
  <done>ZoomControl 타입 정의 완료. NoteChartEditorProps + EditorToolbarProps에 줌 관련 props 추가.</done>
</task>

<task type="auto">
  <name>Task 2: NoteChartEditor.tsx — 커서 고정 줌 + 범위 확장 + 키모드 기본값 + zoomControlRef</name>
  <files>c:/SourceCode/bms-editor/src/chart/NoteChartEditor.tsx</files>
  <read_first>
    - c:/SourceCode/bms-editor/src/chart/NoteChartEditor.tsx
    - c:/SourceCode/bms-editor/src/chart/editor/types.ts
  </read_first>
  <action>
**A. 키모드 기본값 함수 추가** — 파일 상단 import 블록 바로 아래, `EditorCanvas` 함수 정의 전에 추가:

```typescript
/** 키모드별 기본 beatScale (px/beat) */
function defaultBeatScaleForKeyMode(keyMode: string): number {
  if (['4K', '5K', '6K', '7K'].includes(keyMode)) return 20;
  if (['8K', '9K', '10K'].includes(keyMode)) return 15;
  if (['12K', '14K'].includes(keyMode)) return 12;
  if (['18K', '24K'].includes(keyMode)) return 8;
  if (keyMode === '48K') return 4;
  return 20;
}
```

**B. EditorCanvas 함수 시그니처에 새 props 추가** — `bgmChannelCount` 다음 줄에:

```typescript
  zoomControlRef,
  onBeatScaleChange,
```

그리고 타입에 추가 — `}: Omit<NoteChartEditorProps, ...> & {` 블록의 기존 항목들 뒤에 이미 Omit으로 포함되므로 별도 선언 불필요. 단, `Omit<NoteChartEditorProps, 'height' | 'className' | 'hasUnsavedChanges' | 'branchName' | 'noteHeight'>` 이므로 `zoomControlRef`와 `onBeatScaleChange`는 자동 포함됨.

**C. beatScale state 초기화 변경** — 기존:
```typescript
const [beatScale, setBeatScale] = useState(initialBeatScale);
```
변경:
```typescript
const [beatScale, setBeatScale] = useState(() =>
  initialBeatScale === 20 ? defaultBeatScaleForKeyMode(keyMode) : initialBeatScale
);
```

**D. pendingBeatScaleRef 추가** — `pointerUpProcessedRef` 선언 근처(line ~90):
```typescript
const pendingBeatScaleRef = useRef<number | null>(null);
```

**E. handleWheel 수정** — 기존 Ctrl+Wheel 블록:
```typescript
const zoomDelta = e.deltaY > 0 ? -2 : 2;
setBeatScale((prev) => Math.max(5, Math.min(80, prev + zoomDelta)));
```
교체:
```typescript
const factor = e.deltaY > 0 ? (1 / 1.15) : 1.15;
const newScale = Math.max(2, Math.min(200, Math.round(beatScaleRef.current * factor)));
// 커서 고정 줌: 마우스 위치의 beat를 유지
const rect = gl.domElement.getBoundingClientRect();
const cursorYFraction = Math.max(0, Math.min(1, (e.clientY - rect.top) / rect.height));
const oldViewportBeats = viewportBeatsRef.current;
const newViewportBeats = oldViewportBeats * beatScaleRef.current / newScale;
const cursorBeat = scrollBeatRef.current + cursorYFraction * oldViewportBeats;
const newScrollBeat = cursorBeat - cursorYFraction * newViewportBeats;
const maxScroll = Math.max(0, totalBeats - newViewportBeats + 4);
setBeatScale(newScale);
pendingBeatScaleRef.current = newScale;
setScrollBeat(Math.max(0, Math.min(maxScroll, newScrollBeat)));
```

**F. useFrame에 onBeatScaleChange 디바운스 추가** — 기존 useFrame 블록 내 맨 끝에 추가:
```typescript
// rAF 디바운스: pendingBeatScaleRef가 있으면 onBeatScaleChange 호출 후 클리어
if (pendingBeatScaleRef.current !== null) {
  onBeatScaleChange?.(pendingBeatScaleRef.current);
  pendingBeatScaleRef.current = null;
}
```

**G. zoomControlRef 연결 useEffect 추가** — `coordConverterRef` useEffect 바로 다음에:
```typescript
// size와 totalBeats를 ref로 유지 (fitToChart에서 최신값 접근)
const sizeRef = useRef(size);
sizeRef.current = size;
const totalBeatsZoomRef = useRef(totalBeats);
totalBeatsZoomRef.current = totalBeats;

useEffect(() => {
  if (!zoomControlRef) return;
  zoomControlRef.current = {
    zoomIn: () => {
      setBeatScale((prev) => {
        const n = Math.min(200, Math.round(prev * 1.3));
        pendingBeatScaleRef.current = n;
        return n;
      });
    },
    zoomOut: () => {
      setBeatScale((prev) => {
        const n = Math.max(2, Math.round(prev / 1.3));
        pendingBeatScaleRef.current = n;
        return n;
      });
    },
    zoomTo: (scale: number) => {
      const n = Math.max(2, Math.min(200, Math.round(scale)));
      setBeatScale(n);
      pendingBeatScaleRef.current = n;
    },
    fitToChart: () => {
      const ortho = camera as THREE.OrthographicCamera;
      const zoom = ortho.zoom || 1;
      const viewH = sizeRef.current.height / zoom;
      const tb = totalBeatsZoomRef.current;
      const n = tb > 0 ? Math.max(2, Math.min(200, Math.round(viewH / (tb * 1.1)))) : 20;
      setBeatScale(n);
      setScrollBeat(0);
      pendingBeatScaleRef.current = n;
    },
  };
  return () => { if (zoomControlRef) zoomControlRef.current = null; };
}, [zoomControlRef, camera]);
```

**H. NoteChartEditor (외부 컴포넌트) — 새 props 디스트럭처링 및 전달**:

NoteChartEditor 함수 파라미터에 추가:
```typescript
  zoomControlRef, onBeatScaleChange,
```

EditorCanvas 호출에 추가:
```typescript
  zoomControlRef={zoomControlRef} onBeatScaleChange={onBeatScaleChange}
```

**I. ZoomControl 재수출** — 파일 하단 Re-exports 블록에 추가:
```typescript
export type { ZoomControl } from './editor/types';
```
  </action>
  <verify>
    <automated>cd c:/SourceCode/bms-editor && npx tsc --noEmit --skipLibCheck 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - NoteChartEditor.tsx contains `defaultBeatScaleForKeyMode`
    - NoteChartEditor.tsx wheel handler uses `Math.max(2, Math.min(200,` (not 5/80)
    - NoteChartEditor.tsx wheel handler uses `factor` multiplicative (not `zoomDelta`)
    - NoteChartEditor.tsx contains `cursorYFraction` cursor-anchored logic
    - NoteChartEditor.tsx contains `pendingBeatScaleRef`
    - NoteChartEditor.tsx useEffect populates `zoomControlRef.current = {`
    - NoteChartEditor.tsx contains `export type { ZoomControl }`
    - TypeScript no errors
  </acceptance_criteria>
  <done>커서 고정 줌, 범위 2~200, 키모드 기본값, zoomControlRef, onBeatScaleChange 모두 구현 완료.</done>
</task>

<task type="auto">
  <name>Task 3: EditorToolbar.tsx — 줌 버튼 그룹 UI 추가</name>
  <files>c:/SourceCode/bms-editor/src/chart/editor/EditorToolbar.tsx</files>
  <read_first>
    - c:/SourceCode/bms-editor/src/chart/editor/EditorToolbar.tsx
    - c:/SourceCode/bms-editor/src/chart/editor/types.ts
  </read_first>
  <action>
**A. lucide-react import에 ZoomIn, ZoomOut, Maximize2 추가**:

기존 import 줄:
```typescript
import {
  MousePointer2, Plus, Trash2, Move, Music, Gauge, Timer,
  Undo2, Redo2, Copy, Clipboard, Save, GitBranch,
} from 'lucide-react';
```
에 `ZoomIn, ZoomOut, Maximize2,` 추가.

**B. EditorToolbar 함수 파라미터에 새 props 추가** — `onLayerLockToggle` 아래:
```typescript
  onZoomIn,
  onZoomOut,
  onZoomPreset,
  onZoomFit,
  currentBeatScale,
```

**C. EditorToolbar 컴포넌트 내부에 showZoomPreset 상태 추가** — `const tools = [...]` 바로 위에:
```typescript
const [showZoomPreset, setShowZoomPreset] = React.useState(false);
```

**D. 줌 그룹 삽입** — `{/* 노트 두께 */}` 블록과 `{/* 키 모드 */}` 블록 사이에 삽입:

```tsx
{/* 줌 컨트롤 */}
{(onZoomIn || onZoomOut) && (
  <div className="flex items-center gap-1 border-r pr-2">
    <span className="text-xs text-muted-foreground">Z:</span>
    <button
      onClick={onZoomOut}
      disabled={currentBeatScale !== undefined && currentBeatScale <= 2}
      className="p-1 rounded hover:bg-muted transition-colors disabled:opacity-30"
      title="축소 (-)"
    >
      <ZoomOut size={14} />
    </button>
    <div className="relative">
      <button
        onClick={() => setShowZoomPreset((v) => !v)}
        className="w-8 text-[10px] text-center text-muted-foreground hover:text-foreground px-1"
        title="줌 배율 (클릭: 프리셋)"
      >
        {currentBeatScale ?? 20}
      </button>
      {showZoomPreset && (
        <div className="absolute top-full left-1/2 -translate-x-1/2 mt-1 bg-popover border border-border rounded shadow-md z-50 py-1 min-w-[100px]">
          {[
            { label: 'Overview', scale: 5 },
            { label: 'Work', scale: 20 },
            { label: 'Detail', scale: 80 },
          ].map((p) => (
            <button
              key={p.scale}
              onClick={() => { onZoomPreset?.(p.scale); setShowZoomPreset(false); }}
              className={cn(
                'block w-full px-3 py-1 text-xs hover:bg-muted text-left transition-colors',
                currentBeatScale === p.scale && 'text-blue-400'
              )}
            >
              {p.label} <span className="text-muted-foreground">({p.scale})</span>
            </button>
          ))}
        </div>
      )}
    </div>
    <button
      onClick={onZoomIn}
      disabled={currentBeatScale !== undefined && currentBeatScale >= 200}
      className="p-1 rounded hover:bg-muted transition-colors disabled:opacity-30"
      title="확대 (=)"
    >
      <ZoomIn size={14} />
    </button>
    {onZoomFit && (
      <button
        onClick={onZoomFit}
        className="p-1 rounded hover:bg-muted transition-colors"
        title="차트 전체 보기"
      >
        <Maximize2 size={14} />
      </button>
    )}
  </div>
)}
```
  </action>
  <verify>
    <automated>cd c:/SourceCode/bms-editor && npx tsc --noEmit --skipLibCheck 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - EditorToolbar.tsx imports `ZoomIn, ZoomOut, Maximize2`
    - EditorToolbar.tsx contains `showZoomPreset` state
    - EditorToolbar.tsx contains zoom group with ZoomOut/ZoomIn buttons
    - EditorToolbar.tsx contains preset dropdown (Overview/Work/Detail)
    - EditorToolbar.tsx contains `disabled={currentBeatScale !== undefined && currentBeatScale <= 2}` on ZoomOut
    - EditorToolbar.tsx contains `disabled={currentBeatScale !== undefined && currentBeatScale >= 200}` on ZoomIn
    - TypeScript no errors
  </acceptance_criteria>
  <done>툴바에 줌 버튼 그룹 추가 완료. ZoomOut/숫자표시(프리셋드롭다운)/ZoomIn/Fit 구성.</done>
</task>

<task type="auto">
  <name>Task 4: Editor.tsx — zoomControlRef + currentBeatScale 연결</name>
  <files>c:/SourceCode/bms-electron-app/src/renderer/routes/Editor.tsx</files>
  <read_first>
    - c:/SourceCode/bms-electron-app/src/renderer/routes/Editor.tsx (first 300 lines)
    - c:/SourceCode/bms-electron-app/src/renderer/routes/Editor.tsx (lines 1600-1700)
  </read_first>
  <action>
**A. ZoomControl import 추가** — `@rhythm-archive/bms-editor` import 줄에 `ZoomControl` 추가:

기존:
```typescript
import {
  NoteChartEditor,
  EditorToolbar,
  ...
} from '@rhythm-archive/bms-editor';
import type { BmsChartDiffInfo, NoteChartEditorProps } from '@rhythm-archive/bms-editor';
```
`import type { ... }` 줄에 `ZoomControl` 추가:
```typescript
import type { BmsChartDiffInfo, NoteChartEditorProps, ZoomControl } from '@rhythm-archive/bms-editor';
```

**B. zoomControlRef + currentBeatScale state 추가** — 기존 ref 선언들 (예: `zoomControlRef` 다음 적절한 위치, `playbackBeatRef` 근처) 에:

```typescript
const zoomControlRef = useRef<ZoomControl | null>(null);
const [currentBeatScale, setCurrentBeatScale] = useState(20);
```

**C. NoteChartEditorBridge 호출에 props 추가** — Editor.tsx 내 `<NoteChartEditorBridge` 렌더 블록에서 기존 props 다음에 추가:
```tsx
zoomControlRef={zoomControlRef}
onBeatScaleChange={setCurrentBeatScale}
```

**D. EditorToolbar 호출에 줌 props 추가** — 기존 `<EditorToolbar` 렌더 블록에 추가:
```tsx
onZoomIn={() => zoomControlRef.current?.zoomIn()}
onZoomOut={() => zoomControlRef.current?.zoomOut()}
onZoomPreset={(s) => zoomControlRef.current?.zoomTo(s)}
onZoomFit={() => zoomControlRef.current?.fitToChart()}
currentBeatScale={currentBeatScale}
```
  </action>
  <verify>
    <automated>cd c:/SourceCode/bms-electron-app && npx tsc --noEmit --skipLibCheck 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - Editor.tsx imports `ZoomControl`
    - Editor.tsx contains `zoomControlRef = useRef<ZoomControl | null>(null)`
    - Editor.tsx contains `currentBeatScale` state
    - Editor.tsx NoteChartEditorBridge call contains `zoomControlRef={zoomControlRef}`
    - Editor.tsx NoteChartEditorBridge call contains `onBeatScaleChange={setCurrentBeatScale}`
    - Editor.tsx EditorToolbar call contains `onZoomIn` and `onZoomFit` callbacks
    - TypeScript no errors
  </acceptance_criteria>
  <done>Editor.tsx 연결 완료. zoomControlRef → NoteChartEditor, 줌 콜백 → EditorToolbar.</done>
</task>

</tasks>

<verification>
1. TypeScript: `npx tsc --noEmit --skipLibCheck` passes in both bms-editor AND bms-electron-app
2. Functional: ZoomIn/ZoomOut buttons appear in toolbar (Z: ⊟ 20 ⊞ ⊡ 형태)
3. Functional: ZoomOut disabled when scale=2, ZoomIn disabled when scale=200
4. Functional: Clicking scale number shows Overview(5)/Work(20)/Detail(80) dropdown
5. Functional: Ctrl+Wheel zoom keeps cursor beat fixed (no view jump)
6. Functional: Opening 48K chart starts at zoom≈4 (not 20)
</verification>

<success_criteria>
- 확대 버튼: ZoomIn 아이콘, max=200에서 disabled
- 축소 버튼: ZoomOut 아이콘, min=2에서 disabled
- 범위 확장: min=2, max=200 (기존 5~80에서 확장)
- 커서 고정 줌: Ctrl+Wheel 시 마우스 아래 beat가 고정됨
- 프리셋: 숫자 클릭 → Overview/Work/Detail 드롭다운
- Fit-to-Chart: Maximize2 아이콘 버튼으로 전체 차트 보기
- 키모드 기본값: 48K=4, 24K=8, 14K=12, 7K=20 등
- 배율 표시: 현재 px/beat 숫자 툴바에 표시
</success_criteria>

<output>
After completion, create `.planning/phases/08-canvas-zoom/08-01-SUMMARY.md`
</output>
